{
  "name": "makhana-ghar-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:mobile": "turbo run dev --filter=mobile",
    "build:mobile": "turbo run build --filter=mobile",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.7.0"
  }
}
