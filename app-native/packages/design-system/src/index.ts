/**
 * Design System — Public API
 *
 * Re-exports all tokens so consumers can do:
 *   import { colors, typography, spacing } from '@makhana-ghar/design-system';
 */
export {
  colors,
  typography,
  spacing,
  radii,
  shadows,
  animation,
  layout,
  componentSpecs,
} from './tokens';
